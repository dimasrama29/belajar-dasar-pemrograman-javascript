// Membuat array | object constructor
const users = new Array(5)
console.log(users)

const customer = new Array("Dimas", "Setyawan", "Ramadhansyah")
console.log(customer)

// Membuat array | Array.from() 
const foo = Array.from("foo")
console.log(foo)

// Array.form() untuk copy
const customerCopy = Array.from(customer)
console.log(customerCopy)

// Menggunakan array literal
const fruits = ["mango", "apple", "orange"]


/**
 * Array: Struktur data yang menyimpan data terurut
 * Nilai yang disimpan pada array disebut element
 */